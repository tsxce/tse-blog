import { Noto_Sans, Press_Start_2P } from 'next/font/google'

export const noto_sans = Noto_Sans({
  weight: '500',
  subsets: ['cyrillic'],
})

export const arcade_sans = Press_Start_2P({
  weight: '400',
  subsets: ['cyrillic'],
})
